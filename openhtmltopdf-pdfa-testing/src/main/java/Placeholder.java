public class Placeholder {
    /*
     This class is an empty placeholder in order to generate javadocs, which is required by Maven Central for all packages.
     It's a workaround, but a fairly non-problematic one.
     */
}
